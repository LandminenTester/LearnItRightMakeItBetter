{
  "name": "@lir/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright-E2E-Suiten — Szenariokataloge: docs/testing/scenarios/",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "typescript": "^6.0.3"
  }
}
